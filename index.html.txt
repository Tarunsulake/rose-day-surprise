<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Valentine Surprise 💖</title>

<style>
body {
    margin: 0;
    font-family: 'Comic Sans MS', cursive;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
}

/* Gift Box */
.gift-box {
    width: 200px;
    height: 200px;
    background: #ff4d6d;
    position: relative;
    cursor: pointer;
    border-radius: 10px;
    box-shadow: 0 0 20px rgba(0,0,0,0.3);
    transition: transform 0.5s;
}

.gift-box:hover {
    transform: scale(1.1);
}

/* Ribbon */
.gift-box::before {
    content: "";
    position: absolute;
    width: 40px;
    height: 200px;
    background: white;
    left: 80px;
}

.gift-box::after {
    content: "";
    position: absolute;
    height: 40px;
    width: 200px;
    background: white;
    top: 80px;
}

/* Letter Card */
.card {
    position: absolute;
    width: 350px;
    padding: 25px;
    background: white;
    border-radius: 15px;
    text-align: center;
    box-shadow: 0 0 20px rgba(0,0,0,0.2);
    display: none;
    animation: fadeIn 1s forwards;
}

@keyframes fadeIn {
    from {opacity: 0; transform: translateY(50px);}
    to {opacity: 1; transform: translateY(0);}
}

h1 {
    color: #ff4d6d;
}

.funny {
    font-size: 18px;
    margin-top: 10px;
}

/* Floating hearts */
.heart {
    position: absolute;
    color: red;
    font-size: 20px;
    animation: float 5s linear infinite;
}

@keyframes float {
    0% { transform: translateY(0); opacity: 1;}
    100% { transform: translateY(-600px); opacity: 0;}
}
</style>
</head>

<body>

<div class="gift-box" onclick="openGift()"></div>

<div class="card" id="card">
    <h1>Happy Valentine's Day ❤️</h1>
    <p class="funny">
        Roses are red 🌹<br>
        Violets are blue 💙<br>
        I am bad at poetry 😅<br>
        But this website is for you! 💕
    </p>

    <p>
        Warning ⚠️ <br>
        You are now officially promoted to:
        <b>Chief Happiness Officer</b> 😎
    </p>

    <button onclick="showJoke()">Click for Surprise 🤭</button>
    <p id="joke"></p>
</div>

<script>
function openGift() {
    document.querySelector(".gift-box").style.display = "none";
    document.getElementById("card").style.display = "block";
    createHearts();
}

function showJoke() {
    const jokes = [
        "Are you WiFi? Because I am feeling the connection 📶❤️",
        "You must be a magician, because whenever I look at you... everyone else disappears 🎩✨",
        "Are you a charger? Because without you, I would die 🔋😂",
        "You stole my heart… but I will allow it 😌❤️"
    ];

    document.getElementById("joke").innerText =
        jokes[Math.floor(Math.random() * jokes.length)];
}

function createHearts() {
    setInterval(() => {
        const heart = document.createElement("div");
        heart.classList.add("heart");
        heart.innerHTML = "❤️";
        heart.style.left = Math.random() * window.innerWidth + "px";
        heart.style.fontSize = (Math.random() * 20 + 10) + "px";
        document.body.appendChild(heart);

        setTimeout(() => heart.remove(), 5000);
    }, 300);
}
</script>

</body>
</html>
